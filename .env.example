# Dipakai oleh app/api/upload-image/route.ts untuk unggah poster kegiatan & QRIS.
# Disediakan OTOMATIS oleh Vercel setelah Blob store dibuat & disambungkan
# ke project ini (Vercel Dashboard -> Storage -> Create Database -> Blob
# -> Connect ke project masjid-baitul-fadli). Tidak perlu diisi manual di
# Environment Variables — Vercel akan menambahkannya sendiri.
BLOB_READ_WRITE_TOKEN=
